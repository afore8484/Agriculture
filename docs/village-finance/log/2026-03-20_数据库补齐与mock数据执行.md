# 2026-03-20 数据库补齐与 mock 数据执行记录

## 本轮范围
1. 补齐 `village_finance_ops` 中数据结构基线缺失表。
2. 生成最小 mock 数据用于后端联调与回归。
3. 核对“文档表名 -> 实际数据库表名”覆盖率。

## 输入基线
1. `docs/village-finance/data/数据结构文档.md`
2. `modules/village-finance/backend/src/main/resources/db/migration/*.sql`

## 执行内容
1. 新增/更新迁移：
   - `V10__baseline_complement_tables.sql`
2. 新增 mock 脚本：
   - `db/mock/mock_data.sql`
3. 执行脚本（JDBC）：
   - `scripts/ExecuteSqlFile.java`
   - `scripts/QueryDbTables.java`
   - `scripts/QueryTableColumns.java`
   - `scripts/QueryTableCounts.java`
4. 远程库执行：
   - 执行 `V10`
   - 执行 `V6~V9`（补足资产/合同/审批基础表）
   - 执行 `mock_data.sql`

## 核对结果
1. 文档提取表名总数：61
2. 当前库同名覆盖：61/61（missing=0）
3. 关键 mock 行数抽样：
   - `fin_book=1`
   - `fin_period=1`
   - `fin_subject=3`
   - `fin_bank_account=1`
   - `fin_cash_account=1`
   - `fin_voucher_entry=2`
   - `fin_asset_card=1`
   - `fin_contract_main=1`
   - `fin_approve_process=1`
   - `sys_permission=1`
   - `sys_dict=1`

## 阻塞与处理
1. 阻塞：部分历史表 owner 为 `postgres`，直接改结构会报 `must be owner of table`。
2. 处理：V10 改为兼容策略（避免对异构历史表做强依赖结构变更），通过新增补齐表与降耦合外键完成落库。

## 风险提示
1. 远程库存在历史异构表（同名字段不一致），后续需要专门做“现代码 DO 与历史表结构对齐”收口。
2. 当前 mock 以“最小可联调”为目标，不覆盖全量业务场景。
