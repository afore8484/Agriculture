# 2026-03-22 全接口全场景回归基线建立与首轮执行

## 本轮范围

1. 建立全接口回归测试基线（可持续复用）。
2. 执行全接口 L1 路由回归。
3. 校验“接口清单 -> 用例清单”一致性。

## 本轮新增

1. `scripts/generate_finance_api_catalog.ps1`
2. `scripts/generate_finance_regression_cases.ps1`
3. `scripts/run_finance_route_regression.ps1`
4. `scripts/validate_finance_regression_cases.ps1`
5. `docs/village-finance/interface/全接口回归测试基线.md`

## 关键输出

1. `output/finance-api-catalog.csv`（接口目录）
2. `output/finance-regression-cases.csv`（回归用例基线）
3. `output/finance-route-regression-result.csv/json`（L1 执行结果）
4. `output/finance-regression-baseline-validation.json`（一致性校验）

## 执行结果

1. 接口目录：175 条（按代码提取，含双路径兼容接口）。
2. 用例基线：525 条（每接口 3 条：L1 + L2正向 + L2反向）。
3. 一致性校验：通过（缺失/冗余均为 0）。
4. L1 路由回归：175/175 通过。

## 风险与未完成

1. 业务层 L2 全自动化尚未完成，原因：部分环境存在凭证表字段口径与代码模型不一致，触发 `code=500`。
2. 当前 `docs/village-finance/interface/接口文档.md` 对全量接口覆盖不足，需后续按控制器全量补齐。

## 后续建议

1. 先收口凭证相关表字段口径（`fin_voucher`）后，推进 L2 自动化执行。
2. 每轮改动后固定执行：
   - `generate_finance_api_catalog.ps1`
   - `run_finance_route_regression.ps1`
   - `validate_finance_regression_cases.ps1`
