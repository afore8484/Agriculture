# 2026-03-22 全量回归执行（第2轮）
## 执行范围
1. 全接口目录重建（按 Controller 自动提取）。
2. 全接口回归用例基线重建（L1/L2+/- 三层）。
3. L1 全接口路由回归执行。
4. 当前已自动化的 L2 业务场景回归执行（工作台场景）。

## 执行命令
1. `powershell -ExecutionPolicy Bypass -File scripts/generate_finance_api_catalog.ps1`
2. `powershell -ExecutionPolicy Bypass -File scripts/generate_finance_regression_cases.ps1`
3. `powershell -ExecutionPolicy Bypass -File scripts/run_finance_route_regression.ps1`
4. `powershell -ExecutionPolicy Bypass -File scripts/validate_finance_regression_cases.ps1`
5. `powershell -ExecutionPolicy Bypass -File scripts/test_dashboard_api.ps1`

## 结果摘要
1. 接口目录：175 条。
2. 回归用例基线：525 条（175 * 3）。
3. L1 路由回归：175/175 通过。
4. 用例基线完整性：通过（缺失 0、冗余 0）。
5. L2（当前自动化子集，工作台）：12 条，5 通过，7 失败。

## 结果产物
1. `output/finance-api-catalog.csv`
2. `output/finance-regression-cases.csv`
3. `output/finance-route-regression-result.csv`
4. `output/finance-route-regression-result.json`
5. `output/finance-regression-baseline-validation.json`
6. `output/dashboard-api-smoke-result.csv`
7. `output/dashboard-api-smoke-result.json`

## 失败清单（L2 当前自动化子集）
1. `DASH-STAT-001` `/api/village-finance/home/stats`
2. `DASH-CHART-001` `/api/village-finance/home/charts`
3. `DASH-VOUCHER-001` `/api/village-finance/home/recent-vouchers`
4. `DASH-PROGRESS-001` `/api/village-finance/home/progress`
5. `DASH-COMPAT-001` `/api/home/stats`
6. `DASH-COMPAT-002` `/api/home/progress`
7. `DASH-DEP-001` `/api/village-finance/vouchers`

## 已确认根因（关键）
1. 凭证查询 SQL 与 PostgreSQL 实际表字段不一致：SQL 仍按 `id/book_id` 查询，现库口径为 `voucher_id/ledger_id`，导致 `code=500`。
2. 工作台中依赖凭证聚合的接口受上述问题连带失败。

## 回归基线冻结说明（延续）
1. 每次后端改动后，至少执行一次 L1 全接口回归。
2. 业务规则改动后，必须补充并执行对应 L2 用例。
3. 本次结果已落盘，后续直接复用同一命令链。
