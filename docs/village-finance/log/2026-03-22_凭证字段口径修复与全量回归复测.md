# 2026-03-22 凭证字段口径修复与全量回归复测
## 本轮目标
1. 修复凭证接口 `500`（`fin_voucher` 字段口径不一致）。
2. 按同一套命令回跑全量回归（L1 + L2 工作台场景）。

## 本轮变更
1. 新增迁移脚本：`modules/village-finance/backend/src/main/resources/db/migration/V11__voucher_schema_compatibility.sql`
2. 修复测试脚本计数兼容：`scripts/test_dashboard_api.ps1`

## 数据库执行
1. 已执行 `V11__voucher_schema_compatibility.sql` 到 `village_finance_dev`（schema: `village_finance_ops`）。
2. 依赖前置：`fin_voucher`、`fin_voucher_line` owner 调整为 `village_finance` 后执行成功。

## 回归命令（同套）
1. `powershell -ExecutionPolicy Bypass -File scripts/generate_finance_api_catalog.ps1`
2. `powershell -ExecutionPolicy Bypass -File scripts/generate_finance_regression_cases.ps1`
3. `powershell -ExecutionPolicy Bypass -File scripts/run_finance_route_regression.ps1`
4. `powershell -ExecutionPolicy Bypass -File scripts/test_dashboard_api.ps1`
5. `powershell -ExecutionPolicy Bypass -File scripts/validate_finance_regression_cases.ps1`

## 结果对比
1. L1 全接口路由回归：`175/175`（修复前后均为全绿）。
2. L2 工作台场景：
   - 修复前：`12` 条，`5` 通过，`7` 失败。
   - 修复后：`12` 条，`12` 通过，`0` 失败。
3. 用例基线校验：`525/525` 完整（缺失 0、冗余 0）。

## 结果产物
1. `output/finance-route-regression-result.csv`
2. `output/finance-route-regression-result.json`
3. `output/dashboard-api-smoke-result.csv`
4. `output/dashboard-api-smoke-result.json`
5. `output/finance-regression-baseline-validation.json`

## 风险与备注
1. `validate_finance_regression_cases.ps1` 中 `interface_doc_endpoint_lines` 当前仍为 `0`，属于文档计数脚本问题，不影响接口执行结果。
2. 本次为环境修复已人工执行 SQL；后续部署请通过标准 Flyway 流程执行 `V11`，保持环境一致。
