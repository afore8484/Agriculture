# village-finance 开发日志

该目录用于按小时记录 `村委财务事务管理系统` 后端开发阶段日志。

记录规则：
1. 每隔 1 小时至少新增 1 份日志。
2. 日志应与阶段性 `git commit` 对应。
3. 日志只记录 `village-finance` 模块开发，不混入其他模块。

推荐命名：
1. `YYYY-MM-DD_HH-00_阶段日志.md`
2. 如同一小时多次记录，可追加序号。
