# 村委财务事务管理（全量验收审计）

- 审计对象：`https://card-kite-76534082.figma.site`
- 审计时间：2026-03-10（Asia/Shanghai）
- 审计方式：Playwright 真浏览器自动巡检 + 关键流程手工深测
- 覆盖范围：28/28 路由页面（全菜单）

## 一、总体结论
- 页面可达性：**通过**（28/28 页面均可加载）
- 主流程可用性：**基本通过**（审批通过、支付、账户/往来单位新增等可执行）
- 验收结论：**有条件通过（需修复 3 个问题后建议复核）**

## 二、关键审计结果
- 全量路由巡检：`28` 页全部成功加载，无白屏/崩溃。
- 抽测通过项：
  - 审批中心“通过”操作可改变列表状态（待审批减少、已通过增加）。
  - 支付管理“确认支付”后状态由“待支付”变“已支付”。
  - 银农直联“新增账户”“新增往来单位”可落表。
  - 党建学习管理完成率计算正确（显示值=完成人数/参与人数）。

## 三、问题清单（按优先级）

### P1-1 审批通过后未同步到“历史审批”
- 现象：在“审批中心”将 `SP-2026-0089` 审批通过后，跳转“历史审批”页面未检索到该单据。
- 影响：审批流程无法形成“待办 -> 已办 -> 历史归档”闭环，影响审计追溯可信度。
- 建议：审批动作成功后，统一写入历史数据源（或共享状态）并实时刷新历史列表。

### P1-2 往来个人删除按钮无数据变更
- 页面：银农直联 / 往来管理 / 往来个人
- 现象：点击删除仅提示文案，记录条数不变（3 -> 3）。
- 影响：往来个人维护流程不完整。
- 建议：补充确认弹窗 + `setState` 删除逻辑（与往来单位删除保持一致）。

### P2-1 党员管理“组织关系”Tab未体现差异化内容
- 页面：基层党建 / 党员管理
- 现象：切换“党员列表”与“组织关系”后主内容无明显变化（同一套列表视图）。
- 影响：组织关系模块验收可解释性不足。
- 建议：为“组织关系”提供独立视图（转入/转出/调动记录、状态流转、操作入口）。

## 四、非阻断观察
- 采集到若干 `_json/...` 请求 `ERR_ABORTED`，表现为路由切换过程中的中断请求；当前未造成页面功能异常。

## 五、审计产物
- 自动化原始结果：`/Users/apple/Desktop/farmer/.tmp_audit/audit_result.json`
- 页面截图目录：`/Users/apple/Desktop/farmer/.tmp_audit/shots`

