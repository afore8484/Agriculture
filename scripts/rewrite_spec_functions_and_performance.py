from copy import deepcopy
from pathlib import Path
import shutil
import openpyxl
from docx import Document
from docx.oxml import OxmlElement
from docx.table import Table
from docx.text.paragraph import Paragraph

BASE = Path(r"E:\workspaces\Agriculture\prototype")
DOCX_PATH = BASE / "output" / "doc" / "spec_analysis_draft_v1.docx"
BACKUP_PATH = BASE / "output" / "doc" / "spec_analysis_draft_v1.before_33_51_rewrite.docx"
XLSX_PATH = BASE / "tmp" / "spreadsheets" / "contract_compare_1.xlsx"


def insert_paragraph_after(anchor, text=""):
    new_p = OxmlElement("w:p")
    anchor._p.addnext(new_p)
    para = Paragraph(new_p, anchor._parent)
    if text:
        para.add_run(text)
    return para


def clone_table_after(doc, anchor, data):
    sample = doc.tables[0]
    new_tbl = deepcopy(sample._tbl)
    for tr in list(new_tbl.tr_lst):
        new_tbl.remove(tr)
    row_template = deepcopy(sample.rows[0]._tr)
    for _ in data:
        new_tbl.append(deepcopy(row_template))
    anchor._p.addnext(new_tbl)
    table = Table(new_tbl, anchor._parent)
    table.style = sample.style
    for r_idx, row in enumerate(data):
        for c_idx, value in enumerate(row[:4]):
            table.cell(r_idx, c_idx).text = str(value)
    return table


def delete_between(p_start, p_end):
    cur = p_start._p.getnext()
    while cur is not None and cur != p_end._p:
        nxt = cur.getnext()
        cur.getparent().remove(cur)
        cur = nxt


def parse_workbook():
    wb = openpyxl.load_workbook(XLSX_PATH, data_only=True)
    ws = wb['Sheet1']
    cur_mod1 = None
    cur_mod2 = None
    mods = {}
    for r in range(3, ws.max_row + 1):
        m1 = ws.cell(r, 2).value
        m2 = ws.cell(r, 3).value
        contract = ws.cell(r, 4).value
        bid = ws.cell(r, 7).value
        if m1:
            cur_mod1 = str(m1).strip()
        if m2:
            cur_mod2 = str(m2).strip()
        func = contract or bid
        if not cur_mod1 or not cur_mod2 or not func:
            continue
        mods.setdefault(cur_mod1, {}).setdefault(cur_mod2, []).append(str(func).strip())
    return mods


def top_examples(items, n=3):
    seen = []
    for item in items:
        if item not in seen:
            seen.append(item)
        if len(seen) >= n:
            break
    return '?'.join(seen)


def module_table(mods, module_name):
    rows = [["????", "?????", "????", "????"]]
    for sub, items in sorted(mods[module_name].items(), key=lambda kv: (-len(kv[1]), kv[0])):
        rows.append([
            sub,
            str(len(items)),
            top_examples(items),
            f"??{sub}?????????????????????????????"
        ])
    return rows


def supervision_table(mods, supervision_modules):
    rows = [["??/??", "????", "?????", "????"]]
    for module_name in [
        module_121,
        module_27,
        module_30,
    ]:
        for sub, items in sorted(mods[module_name].items(), key=lambda kv: (-len(kv[1]), kv[0])):
            short_name = module_name.replace('????????????????-', '')
            rows.append([short_name, sub, str(len(items)), top_examples(items)])
    return rows


def find_after(paragraphs, prefix, start_idx):
    for i in range(start_idx, len(paragraphs)):
        if paragraphs[i].text.strip().startswith(prefix):
            return i
    raise ValueError(prefix)


def main():
    if not BACKUP_PATH.exists():
        shutil.copy2(DOCX_PATH, BACKUP_PATH)

    mods = parse_workbook()
    total_map = {k: sum(len(v) for v in subs.values()) for k, subs in mods.items()}
    module_242 = next(k for k,v in total_map.items() if v == 242)
    module_220 = next(k for k,v in total_map.items() if v == 220)
    module_107 = next(k for k,v in total_map.items() if v == 107)
    module_110 = next(k for k,v in total_map.items() if v == 110)
    module_141 = next(k for k,v in total_map.items() if v == 141)
    module_121 = next(k for k,v in total_map.items() if v == 121)
    module_27 = next(k for k,v in total_map.items() if v == 27)
    module_30 = next(k for k,v in total_map.items() if v == 30)
    doc = Document(DOCX_PATH)
    paragraphs = doc.paragraphs

    i331 = find_after(paragraphs, '3.3.1 ', 90)
    i332 = find_after(paragraphs, '3.3.2 ', i331 + 1)
    i333 = find_after(paragraphs, '3.3.3 ', i332 + 1)
    i334 = find_after(paragraphs, '3.3.4 ', i333 + 1)
    i335 = find_after(paragraphs, '3.3.5 ', i334 + 1)
    i336 = find_after(paragraphs, '3.3.6 ', i335 + 1)
    i337 = find_after(paragraphs, '3.3.7 ', i336 + 1)
    i4 = find_after(paragraphs, '4. ', i337 + 1)
    i51 = find_after(paragraphs, '5.1 ', 160)
    i52 = find_after(paragraphs, '5.2 ', i51 + 1)

    p331 = paragraphs[i331]
    p332 = paragraphs[i332]
    p333 = paragraphs[i333]
    p334 = paragraphs[i334]
    p335 = paragraphs[i335]
    p336 = paragraphs[i336]
    p337 = paragraphs[i337]
    p4 = paragraphs[i4]
    p51 = paragraphs[i51]
    p52 = paragraphs[i52]

    for start, end in [(p331, p332), (p332, p333), (p333, p334), (p334, p335), (p335, p336), (p336, p337), (p337, p4), (p51, p52)]:
        delete_between(start, end)

    # 3.3.1 ??
    p = insert_paragraph_after(p331, '??????????????????????????????????????????????????????????????????????????????????????????????????????????????????????998????????????????????????')
    platform_rows = [
        ['???', '????', '????', '????'],
        ['???????', '??????', '????????????????????????????????', '???????????????'],
        ['?????????', '?????????????', '??????????????????????', '????????????????'],
        ['?????????', '???????????????', '????????????????????', '??????????????'],
        ['???????', '???????????', '???????????????????????', '????????????'],
    ]
    clone_table_after(doc, p, platform_rows)

    # 3.3.2 ?????
    p = insert_paragraph_after(p332, '??????????????7??????242???????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, module_table(mods, module_242))

    # 3.3.3 ??????????
    p = insert_paragraph_after(p333, '???????????????????7??????220?????????????????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, module_table(mods, module_220))

    # 3.3.4 ??
    p = insert_paragraph_after(p334, '????????????????????7??????107??????????????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, module_table(mods, module_107))

    # 3.3.5 ????
    p = insert_paragraph_after(p335, '????????????????????9??????110????????????????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, module_table(mods, module_110))

    # 3.3.6 ????
    p = insert_paragraph_after(p336, '??????????????????15??????141????????????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, module_table(mods, module_110))

    # 3.3.7 ??????
    total_supervision = sum(sum(len(v) for v in mods[m].values()) for m in [
        module_121,
        module_27,
        module_30])
    p = insert_paragraph_after(p337, f'??????????????????????????????????????????{total_supervision}??????????????????????????????????????????????????????????????????????????????????????????????????????????')
    clone_table_after(doc, p, supervision_table(mods, [module_121, module_27, module_30]))

    # 5.1 ????
    p = insert_paragraph_after(p51, '?????????????????????????????????????????????????????????Java 17+ ????????????????????????????????')
    p = insert_paragraph_after(p, '5.1.1 ????')
    perf_rows = [
        ['????', '????', '???', '????'],
        ['????', '?????????', '2000????', '??????????????????'],
        ['?????', '????????????????', '??1????3?', '??????????????????'],
        ['?????', '?????????????2000??', '??5????10?', '??????2000???????'],
        ['?????', '????', '??1?', '??????????????????'],
        ['?????', '????', '??3?', '?????????????????????'],
        ['?????', '????', '??3?', '?????????????????'],
        ['??????', '??????????', '??1?', '??????????????'],
        ['??????', '100?????????', '??3?', '???100??????????'],
    ]
    clone_table_after(doc, p, perf_rows)
    p = insert_paragraph_after(p, '5.1.2 ???????')
    cond_rows = [
        ['????', '????', '????', '??'],
        ['????', '?????????????????????', '?????????', '???????????'],
        ['????', '?????????????????????????????', '??????', '????????????'],
        ['????', '???????????????????????????', '?????????', '??????????'],
        ['?????', '??????????????????', '??????????', '???????????'],
        ['????', '????????????????????', '?????????????', '??????????????'],
    ]
    clone_table_after(doc, p, cond_rows)
    insert_paragraph_after(p, '??????????????????????????????????????????????????????????')

    doc.save(DOCX_PATH)
    print('updated')


if __name__ == '__main__':
    main()
